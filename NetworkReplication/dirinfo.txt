
 ѕример проекта , имеющего дело с реппликацией при помощи
 передачи данных через сокеты по протоколу TCP/IP.
 —одержит клиента и сервера TCP/IP.
 ¬ библиотеке наход€тс€ два основных класса Source и Destination
  ласс Source получает строку соединени€ с базой данных.
 «на€ ннаименование таблицы обращаетс€ к метаданным (INFORMATION_SCHEMA),
 считывает наименовани€ колонок и их типы, формирует на оснровании этого 
 запрос к таблице, считывает данные дл€ репликации, сериализует их объект 
 дл€ передачи по сети и клиент отправл€ет эти данные.

 —ервер TCP/IP получает эти данные , объект класса Destination десериализует полученные данные
 и заносит их в нужную таблицу, сформировав запрос дл€ вставки на основе метаданных.

 